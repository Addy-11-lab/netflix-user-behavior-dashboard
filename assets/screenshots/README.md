# Screenshot checklist

Capture these three views after launching the local Streamlit app. Keep the browser at a desktop width and hide personal browser chrome before capture.

1. `overview.png` — KPI cards, executive summary, and both overview charts.
2. `retention-churn.png` — the churn-segmentation chart and insight card.
3. `recommendations.png` — the filter-aware PM recommendation cards.

Replace this file with the three PNGs before publishing so the repository keeps only current visuals.
